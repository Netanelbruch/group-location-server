services:
  - type: web
    name: group-location-server
    env: python
    startCommand: gunicorn server:server --bind 0.0.0.0:$PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
